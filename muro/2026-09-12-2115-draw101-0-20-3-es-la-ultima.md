de:    draw101
para:  jr
qué:   aviso — draw101 0.20.3 es la última; la rama `claude/0.20.3` reemplaza a `claude/0.20.2-negativos`

# draw101 0.20.3 es la última (Mike, 12-sep)

Reemplaza al recado `2026-09-12-1930`: **no apliques la 0.20.2**, aplica la 0.20.3,
que la incluye. Mike lo pidió así: «sólo avisa a Jr. que la 0.20.3 es la última».

- Rama **`claude/0.20.3`** en `draw101`: `claude/0.20.3.patch` (diff acumulado de
  `ui/entrada.js`, `ui/vista.js`, `ui/editar.js`, `core/version.py`, `package.json`
  contra `main` = X.0.2; aplica limpio con `git apply`) + `pruebas/t016`, `t017`,
  `t018` enteras. Instrucciones en `claude/0.20.3-LEEME.md`.
- Medido con el parche aplicado: **18 pruebas, 382 comprobaciones, todas verdes.**
- Instalador `draw101-0.20.3-setup.exe`: 119 252 351 B, sha256
  `54cebab5c3b8984d28b803485017c5bd669fc461880ae17b7a3d9a5848e1a1e2`. Entregado
  a Mike por el chat. **Pendiente en `descargas`**: release `draw101-0.20.3`, tag
  `draw101-ultima`, `draw101.json`. Nadie más lo puede hacer.

Qué trae encima de 0.20.2: el valor tecleado manda sobre el snap (una sola causa,
`puntoDelCursor` devolvía el snap antes del bloqueo); ESCALARD, escala en una sola
dirección (círculo → elipse exacta con borrar+agregar en la misma operación).
Queda para 0.20.4: TRIM en polilínea (RECORTAR hoy sólo admite líneas y arcos).

Todo lo demás del recado 1930 (respuesta al sitio, medición de `dwgjs/node_modules`,
receta de armado en Linux) sigue vigente.
