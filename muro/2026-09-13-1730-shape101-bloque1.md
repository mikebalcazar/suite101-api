# shape101 · bloque 1 arrancado: el contrato del motor

de: chat de shape101
para: chat de draw101, chat del sitio, Jr.
qué: shape101 ya tiene chat propio y el bloque 1 del plan está encargado

**Decisiones de Mike del 13-sep** (botones, una por vez):

1. Un documento de shape101 guarda **una pieza**. El ensamble será un documento
   aparte que referencie piezas, más adelante.
2. **mm con centésimas**: el kernel trabaja exacto, lo que se teclea y se muestra
   va a 0,01.
3. La pieza guarda **material y espesor** como dato, aparte de la geometría
   (para el despiece y el costeo de después).

**Qué quedó en el repo** (`main` 9d6ea454, PR #4): la prueba de aceptación del
contrato del motor, escrita antes de programarlo —`app/pruebas/t01_documento.py`
y el arnés `app/verificar.py`—. Falla a propósito: `app/motor/` todavía no
existe. Fija el archivo `.s101` (bocetos embebidos), el historial editable con
caché por operación y la API local (`/api/salud`, `/api/documento`,
`/api/operacion`, `/api/modelo` con una malla por cara, `/api/exportar`).

**Jr.**: el encargo está en Drive, `suite101/shape101/`,
«shape101-encargo-b1-contrato-motor-2026-09-13». Pide mover el kernel de `poc/`
a `app/motor/` sin duplicarlo; `poc/verificar.py` tiene que seguir en verde con
sus 80 comprobaciones.

**draw101**: shape101 no toca draw101, sólo lee y escribe su `.t101d`. Si el
formato cambia, avísalo aquí.
