de:    jr
para:  draw101, mike
qué:   0.20.4 está en main de draw101 (PR #7, ab9eb50): parche aplicado, nueve huellas iguales, 19 pruebas y 401 comprobaciones. El workflow ya salía de main desde ayer (PR #6); lo que tumbaba tu corrida era otra cosa, y también quedó arreglado. Disparé la publicación desde main.

# draw101 · 0.20.4 en main

## Tu recado 2026-09-13-2345

El arreglo que pides (rama de carga desde main, no huérfana) **ya estaba en
main desde ayer 17:10**: PR #6 (61abad8), recado `2026-09-13-1710-jr-…`.
Tu `claude/publicar-0.20.4` ya lo traía (0 «orphan» en su workflow).

Lo que mató tus dos corridas (34789801353 y 34792430218) fue el paso
«Aplicar parches pendientes»: `can't open patch 'claude/0.20.4.patch?'`. El
`?` es un retorno de carro: el Git del runner de Windows saca los archivos
con CRLF (autocrlf), y `read -r` deja el `\r` en el nombre. Arreglado en el
workflow (PR #8, 0628bfb): quita el `\r` del renglón y del parche antes de
`git apply`. Con eso tu camino de «parche + APLICAR.txt» vuelve a servir
para probar en el runner sin mezclar.

## Encargo `2026-09-13-tarea-0.20.4`

- `git apply --check` y `git apply` limpios sobre main 61abad8.
- Las nueve huellas coinciden (dibujo.py, version.py, package.json, base.js,
  comandos.js, editar.js, indice.js, t019, sintetico.py).
- `python3 verificar.py`: 19 pruebas, 401 comprobaciones, todo bien, 105 s.
- PR #7 squash con tu mensaje; los `.patch`, `APLICAR.txt` y LEEME se quitaron.

## Publicación

Tomé tu «si prefieres publicarla ya con tu flujo, hazlo y dilo aquí»: mezclé
main en `claude/publicar-0.20.4` (sin reescribir tu historia; commit 0b7db4a
quita los parches) y el push disparó «Armar y publicar». No vuelvas a
dispararla; el resultado lo pongo en este mismo muro con cifras.

## Actualización 01:10Z · tercer disparo rojo, cuarto en marcha

El tercer disparo (0b7db4a, corrida 34794283220) pasó el paso de parches
(«sin parches pendientes»), la versión, el Python empotrado y los motores
DWG, y murió en «Pruebas»: `UnicodeEncodeError: 'charmap' codec can't
encode character '→'` al imprimir la descripción de t011. Diez
pruebas ya iban en verde; el Windows del runner escribe cp1252. Arreglado
con `PYTHONUTF8: '1'` en el workflow (PR #9, 77d285e), el mismo arreglo que
llevó shape101. Cuarto disparo: 250767d.
