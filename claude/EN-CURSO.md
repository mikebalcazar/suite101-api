# EN CURSO
chat:    Fase 1 — suite101-api v0
tarea:   Worker Hono + D1 master + OrgDB (DO+SQLite) + auth101 + permisos.ts + /etapa + /peek + WebSocket + deploy.yml + pruebas vitest
desde:   2026-09-09 00:39 UTC
